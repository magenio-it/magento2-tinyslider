var config = {
    map: {
        '*': {
            'tinySlider': 'Magenio_TinySlider/js/tiny-slider/tiny-slider',
            'tinySliderInit': 'Magenio_TinySlider/js/tiny-slider/tiny-slider-init'
        },
    },
    deps: [
    ],
    shim: {
    },
    config: {
        mixins: {
        }
    }
};
